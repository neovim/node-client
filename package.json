{
  "private": true,
  "workspaces": [
    "packages/neovim",
    "packages/decorators",
    "packages/example-plugin",
    "packages/example-plugin-decorators"
  ],
  "homepage": "https://github.com/neovim/node-client",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^1.4.2",
    "@typescript-eslint/parser": "^1.4.2",
    "eslint": "^5.15.1",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-config-prettier": "^4.1.0",
    "eslint-plugin-import": "^2.16.0",
    "eslint-plugin-prettier": "^3.0.1",
    "husky": "^1.3.1",
    "lerna": "^3.13.1",
    "lint-staged": "^3.6.1",
    "prettier": "^1.12.1",
    "typescript": "^3.3.3333"
  },
  "scripts": {
    "bootstrap": "yarn lerna bootstrap",
    "build": "yarn lerna run build",
    "dev":"yarn lerna run --stream --parallel dev",
    "test": "yarn lerna run test",
    "test-build": "yarn lerna run build && yarn lerna run test-build",
    "test-staged": "yarn lerna run test-staged",
    "test-missing-apis": "yarn lerna run test-missing-apis",
    "test-lint": "yarn lerna run test-lint",
    "test-staged": "yarn lerna run test-staged -- --",
    "lint": "yarn lerna run lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,js}": [
      "eslint --fix",
      "yarn test-staged"
    ]
  },
  "toolchain": {
    "node": "8.15.1",
    "yarn": "1.13.0"
  }
}
