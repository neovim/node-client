codecov:
  notify:
    require_ci_to_pass: no
  ci:
    - appveyor
    - travis

coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project: yes
    patch: yes
    changes: no

parsers:
  javascript:
    enable_partials: yes

comment: off
