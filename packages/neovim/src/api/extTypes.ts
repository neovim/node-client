// The `ExtType` classes, apart from `types.ts`: the classes import that, so it cannot import them.
export { Buffer } from './Buffer';
export { Window } from './Window';
export { Tabpage } from './Tabpage';
